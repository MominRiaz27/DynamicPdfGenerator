<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>

    <style>
        .custom-container {
            height: 100px; /* Set the desired height */
        }
    </style>
</head>
<body>
    <body>
    <div class="container d-flex flex-column justify-content-between align-items-center">
        <h1 class="text-center font-weight-bold mb-4">
            {{title}}
        </h1>

        <!-- User Details Table -->
        <table class="table mb-4" style="width: 50%; border: 3px solid green;">
            
            <tr>
                <th>Name:</th>
                <td>{{user.name}}</td>
            </tr>
            <tr>
                <th>Age:</th>
                <td>{{user.age}}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{user.country}}</td>
            </tr>
            
        </table>

        <!-- Application Status and Button -->
        <div class="d-flex justify-content-center mb-4">
        <p class="mr-3">Application Status:</p>
        {{#equal user.status 'Approved'}}
            <button class="btn-primary">
                {{user.status}}
            </button>
        {{else}}
            <button class="btn-danger">
                {{user.status}}
            </button>
        {{/equal}}
        </div>


        <!-- Paragraph -->
        <div class="mb-4">
            <p>We're Keen to Providing Best Services Kindly Cooperate:</p>
        </div>

        <!-- Clickable URL Links -->
        <div class="d-flex justify-content-center">
            <a href="https://google.com" target="_blank" class="mr-3">Click me!</a>
            <a href="https://example.com" target="_blank">Click me too!</a>
        </div>
    </div>
</body>

</body>
</html>